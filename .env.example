# Cloudflare tunnel token for the `home-server` tunnel.
# Get a fresh one from: Cloudflare Zero Trust → Networks → Tunnels → home-server → Edit → Connectors (Docker tab) → token in --token flag
CLOUDFLARE_TUNNEL_TOKEN=
